<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DicodingStory" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="colorPrimary">@color/blue_grey</item>
        <item name="colorSecondary">@color/white</item>
        <item name="colorAccent">@color/grey_blue</item>
        <item name="colorSecondaryContainer">@color/transparent</item>

        <item name="android:windowIsTranslucent">true</item>

        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@android:transition/explode</item>
        <item name="android:windowExitTransition">@android:transition/explode</item>
    </style>

    <style name="Theme.DicodingStory" parent="Base.Theme.DicodingStory" />
    
    <style name="Widget.DicodingStory.FloatingActionButton" parent="Widget.Material3.FloatingActionButton.Large.Primary">
        <item name="backgroundTint">?attr/colorPrimary</item>
        <item name="elevation">4dp</item>
        <item name="fabCustomSize">70dp</item>
        <item name="android:src">@drawable/ic_add_24</item>
        <item name="tint">?attr/colorSecondary</item>
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.Material3.Corner.Full</item>
    </style>
</resources>